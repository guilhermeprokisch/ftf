import sys

# Read and consume the input
line = "Hello"
line = line.rstrip()  # Remove trailing newline character
# print(line)

# After the script finishes, write to stdout
sys.stdout.write("Script has finished running.")
